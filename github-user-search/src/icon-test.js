// Simple test to see what's actually in react-icons/gi
import * as GiIcons from 'react-icons/gi';

// Just log the object to see the structure
console.log('React Icons GI:', GiIcons);
console.log('Type of GiIcons:', typeof GiIcons);